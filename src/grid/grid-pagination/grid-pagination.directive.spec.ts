import { GridPaginationDirective } from './grid-pagination.directive';

describe('GridPaginationDirective', () => {
  it('should create an instance', () => {
    const directive = new GridPaginationDirective();
    expect(directive).toBeTruthy();
  });
});
